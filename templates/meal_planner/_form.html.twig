{{ form_start(form) }}

{{ form_row(form.scheduledFor) }}
{{ form_row(form.mealTime) }}
{{ form_row(form.servings) }}

<button class="btn">
    {{ button_label|default('Save') }}
</button>

{{ form_end(form) }}